<div class="span11">
    {{ lansole_pages_block(page, 'title', { 'tag': 'h1' }) }}

    {{ lansole_pages_block(page, 'description', { 'type': 'textarea' }) }}
</div>

<div class="span5">
    {{ lansole_pages_block(page, 'sidebar-title', { 'tag': 'h3' }) }}

    {{ lansole_pages_block(page, 'sidebar-description', { 'type': 'textarea' }) }}
</div>