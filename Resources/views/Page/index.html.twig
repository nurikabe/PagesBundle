{% extends 'LansolePagesBundle:Core:layout.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block container %}
<div class="row">
  <div class="span16">
    <h1>{{ page.title }}</h1>
  </div>
</div>
{% endblock %}